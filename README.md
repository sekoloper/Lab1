# Lab1
Программа представляет из себя лабораторную работу под номером 1 по дисциплине «Язык программирования C#». Лабораторная состоит из двадцати различных задач, каждая по одной из четырёх тем (Методы, Условия, Циклы, Массивы).

## Требования
* Windows 10 (или выше)
* Visual Studio

## Установка и запуск программы
1. Клонируйте репозиторий в Visual Studio
2. Соберите решение (Ctrl+Shift+B)
3. Запустите (Ctrl+F5)

## Руководство по использованию
1. Введите номер задачи.
2. Следуйте дальнейшим инструкциям внутри задачи.
3. После выполнения задачи введите номер новой или 0 для выхода из программы.

## Структура проекта
Lab1
* Program.cs
* Tasks.cs
* InputValidation.cs
* Print.cs

Program.cs – основной файл, содержащий класс Program и метод Main. В нём в консоль выводится список задач и запрашивается номер задачи, после чего вызывается метод соответствующей задачи из класса Tasks.

Tasks.cs – файл, в котором содержатся методы задач в классе Tasks. Каждой задачи соответствует два метода. В методе с нижним регистром выполняется основная логика, а метод с верхним регистром запрашивает значения переменных, обращается к методу с нижним регистром и выводит результат. Для проверки входных данных метод обращается к классу InputValidation.

InputValidation.cs – файл, содержащий статический класс для проверки входных данных InputValidation. Содержит методы для ввода числа типа Int32, числа типа Int32 с нижней и верхней границами значений, статического массива чисел типа Int32, символа типа Char.

Print.cs – файл, содержащий статический класс для вывода статического массива чисел типа Int32 Print и метод для этого соответственно.

## Задачи

### 1 (1.2). Сумма знаков

Формулировка: Дана сигнатура метода: public int sumLastNums (int x); Необходимо реализовать метод таким образом, чтобы он возвращал результат сложения двух последних знаков числах, предполагая, что знаков в числе не менее двух.

Алгоритм: Остаток от деления на 10 числа x складывается с остатком от деления на 10 частного x и 10, эта сумма возвращается.

Примеры:

x = 125

результат: 7

x = 8

результат: вывод в консоль “Введите число, не превышающее размерность int (2.147.483.647 по модулю) и состоящее из двух или более разрядов:” и запрос нового числа (здесь и далее – сам метод не вызывается в основном файле, проверка осуществлена в методе с аналогичным названием но с первой буквой в верхнем регистре и без параметров (к примеру SumLastNums() для sumLastNums(int x)))

### 2 (1.4). Есть ли позитив

Формулировка: Дана сигнатура метода: public bool isPositive (int x); Необходимо реализовать метод таким образом, чтобы он принимал число x и возвращал true, если оно положительное.

Алгоритм: Метод возвращает результат сравнения x с 0 (больше или равно).

Примеры:

x = 67

результат: true

x = -98

результат: false

### 3 (1.6). Большая буква.

Формулировка: Дана сигнатура метода: public bool isUpperCase (char x); Необходимо реализовать метод таким образом, чтобы он принимал символ x и возвращал true, если это большая буква в диапазоне от ‘A’ до ‘Z’. 

Алгоритм: Метод возвращает результат умножения двух сравнений x (x > ‘A’ и x < ‘Z’)

Примеры:

x = ‘P’

результат: true

x = ‘6’

результат: false

### 4 (1.8). Делитель

Формулировка: Дана сигнатура метода: public bool isDivisor (int a, int b); Необходимо реализовать метод таким образом, чтобы он возвращал true, если любое из принятых чисел делит другое нацело.

Алгоритм: Метод возвращает сложение результатов двух сравнений частных с 0 (частное a и b и частное b и a)

Примеры:

a = 3 b = 9

результат: true

a = 6 b = 7

результат: false

### 5 (1.10) Многократный вызов.

Формулировка: Дана сигнатура метода: public int lastNumSum(int a, int b); Необходимо реализовать метод таким образом, чтобы он считал сумму цифр двух чисел из разряда единиц. Выполните с его помощью последовательное сложение пяти чисел и результат выведите на экран. Постарайтесь выполнить задачу, используя минимально возможное количество вспомогательных переменных.

Алгоритм: Метод возвращает сумму остатков от деления на 10 двух переданных чисел. Для выполнения задания с минимальным количеством переменных, было решено использовать две переменной типа int и одну типа string. Последовательно вводятся числа, результат выполнения метода записывается в строку. После ввода всех чисел выводится строка с вычислениями и итоговым ответов.

Примеры:

x1 = 12 x2 = 23 x3 =34 x4 = 45 x5 = 56

результат:

12+23 это 5

5+34 это 9

9+45 это 14

14+56 это 10

Итого 10

x1 = 11 x2 = 22 x3 = 33 x4 = 44 x5 = 55

результат:

11+22 это 3

3+33 это 6

6+44 это 10

10+55 это 5

Итого 5

### 6 (2.2) Безопасное деление.

Формулировка: Дана сигнатура метода: public double safeDiv (int x, int y); Необходимо реализовать метод таким образом, чтобы он возвращал деление x на y, и при этом гарантировал, что не будет выкинута ошибка деления на 0. При делении на 0 следует вернуть из метода число 0.

Алгоритм: Если y равно 0, возвращается 0. Иначе возвращается результат деления с явным приведением x к double перед операцией. 

Примеры:

x = 3 y = 2

результат: 1.5

x = 9 y = 0

результат: 0

### 7 (2.4) Строка сравнения.

Формулировка: Дана сигнатура метода: public String makeDecision (int x, int y); Необходимо реализовать метод таким образом, чтобы он возвращал строку, которая включает два принятых методом числа и корректно выставленный знак операции сравнения (больше, меньше, или равно).

Алгоритм: Если x и y равны, возвращается строка со знаком равенства. Если x больше y, со знаком больше. Иначе со знаком меньше.

Примеры:

x = 6 y = 9

результат: 6 < 9

x = 56 y = 40

результат: 56 > 40

x = 8 y = 8

результат: 8==8

### 8 (2.6) Тройная сумма

Формулировка: Дана сигнатура метода: public bool sum3 (int x, int y, int z); Необходимо реализовать метод таким образом, чтобы он возвращал true, если два любых числа (из трех принятых) можно сложить так, чтобы получить третье.

Алгоритм: Рассматриваются три выражения: x + y == z, x + z == y и y + z == x. Если хотя бы одно из них истинно, возвращается true, иначе false.

Примеры:

x = 6 y = 5 z = 1

результат: true

x = 1 y = 7 z = 99

результат: false

### 9 (2.8) Возраст.

Формулировка: Дана сигнатура метода: public String age (int x); Необходимо реализовать метод таким образом, чтобы он возвращал строку, в которой сначала будет число х, а затем одно из слов: год года лет. Слово “год” добавляется, если число х заканчивается на 1, кроме числа 11. Слово “года” добавляется, если число х заканчивается на 2, 3 или 4, кроме чисел 12, 13, 14. Слово “лет” добавляется во всех остальных случаях.

Алгоритм: если остаток от деления на 10 равен 1 и x не равен 11, возвращает строку со словом год. Иначе если остаток от деления равен 2, 3 или 4 и при этом x не равно 12, 13 или 14, строка со слов года. Иначе строка со словом лет.

Примеры:

x = 20

результат: 20 лет

x = 54

результат: 54 года

x = 1

результат: 0 лет

### 10 (2.10) Вывод дней недели.

Формулировка: Дана сигнатура метода: public void printDays (String x); В качестве параметра метод принимает строку, в которой записано название дня недели. Необходимо реализовать метод таким образом, чтобы он выводил на экран название переданного в него дня и всех последующих до конца недели дней. Если в качестве строки передан не день, то выводится текст “это не день недели”. Первый день понедельник, последний – воскресенье. Вместо if в данной задаче используйте switch.

Алгоритм: switch принимает строку с пониженным регистром и в зависимости от значения выводит заготовленную строку с оставшимися днями недели. Если строка не является днём недели, будет выведено соответствующее сообщение. 

Примеры:

x = суббота

результат:

суббота

воскресенье

x = сббт

результат: это не день недели

### 11 (3.2). Числа наоборот.

Формулировка: Дана сигнатура метода: public String reverseListNums (int x); Необходимо реализовать метод таким образом, чтобы он возвращал строку, в которой будут записаны все числа от x до 0 (включительно).

Алгоритм: Цикл от x до 0 записывает значение счётчика в строку, строка возвращается.

Примеры:

x = 5

результат: 5 4 3 2 1 0

x = 1

результат: 1 0

### 12 (3.4). Степень числа.

Формулировка: Дана сигнатура метода: public int pow (int x, int y); Необходимо реализовать метод таким образом, чтобы он возвращал результат возведения x в степень y.

Алгоритм: Создаётся переменная z типа int со значением 1, в которую посредством цикла y раз записывается умножение z на x. Если получается число, превышающее размерность int, выводится соответствующее значение.

Примеры:

x = 5 y = 6

результат: 15625

x = 10 y = 10

результат: Ошибка! Результат превышает размерность int.

### 13 (3.6). Одинаковость

Формулировка: Дана сигнатура метода: public bool equalNum (int x); Необходимо реализовать метод таким образом, чтобы он возвращал true, если все знаки числа одинаковы, и false в ином случае.

Алгоритм: посредством получения остатка от деления на 10 между собой сравниваются попарно подряд идущие разряды числа. Если хоть раз они не совпадают, возвращается false, иначе true.

Примеры:

x = 3333

результат: true

x = 123456

результат: false

### 14 (3.8). Левый треугольник.

Формулировка: Дана сигнатура метода: public void leftTriangle (int x); Необходимо реализовать метод таким образом, чтобы он выводил на экран треугольник из символов ‘\*’ у которого х символов в высоту, а количество символов в ряду совпадает с номером строки.

Алгоритм: вызываются два цикла. Вложенный в него печатает символы ‘\*’, а внешний – переход каретки в следующую строку.

Примеры:

x = 4

результат:

\*

**

\***

\*\***

x = 2

результат:

\*

**

### 15 (3.10). Угадайка.

Формулировка: Дана сигнатура метода: public void guessGame(); Необходимо реализовать метод таким образом, чтобы он генерировал случайное число от 0 до 9, далее считывал с консоли введенное пользователем число и выводил, угадал ли пользователь то, что было загадано, или нет. Метод запускается до тех пор, пока пользователь не угадает число. После этого выведите на экран количество попыток, которое потребовалось пользователю, чтобы угадать число.

Алгоритм: В переменную записывается случайное число от 0 до 9, в другую количество попыток. Пока пользователь не введёт число, идентичное загаданному, число попыток будет увеличиваться. После того, как пользователь введёт правильное число, в консоль будет выведено количество попыток.

Пример:

Введите число от 0 до 9: 

6

Вы не угадали, введите число от 0 до 9: 

2

Вы угадали! 

Вы отгадали число за 2 попытки

### 16 (4.2). Поиск последнего значения.

Формулировка: Дана сигнатура метода: public int findLast (int[] arr, int x); Необходимо реализовать метод таким образом, чтобы он возвращал индекс последнего вхождения числа x в массив arr. Если число не входит в массив – возвращается -1.

Алгоритм: Посредством цикла выполняется проход массива с конца. Если число найдено – возвращается его индекс, иначе -1.
Примеры:

arr = [1, 2, 3, 4, 2, 2, 5]

x = 2

результат: 5

arr = [4, 6, 7]

x = 9

результат: -1

### 17 (4.4). Добавление в массив.

Формулировка: Дана сигнатура метода: public int[]add (int[] arr, int x, int pos); Необходимо реализовать метод таким образом, чтобы он возвращал новый массив, который будет содержать все элементы массива arr, однако в позицию pos будет вставлено значение x.

Алгоритм: Создаётся новый массив длиной прошлого плюс 1. Элемента старого массива с начала до позиции нового числа переносится в новый массив. По указанной позиции записывается новое число. До конца старого массива числа опять переносятся в новый позицией плюс 1.

Пример:

arr = [1, 2, 3, 4, 5] 

x = 9 

pos = 3 

результат: [1, 2, 3, 9, 4, 5]

### 18 (4.6). Реверс.

Формулировка: Дана сигнатура метода: public void reverse (int[] arr); Необходимо реализовать метод таким образом, чтобы он изменял массив arr. После проведенных изменений массив должен быть записан задом-наперед.

Алгоритм: Через цикл выполняется проход по массиву до середины, элементы с индексами [размер массива – счётчик - 1] и  [счётчик] меняются местами, используя в качестве буфера вспомогательную переменную. После этого цикл выводится в консоль.

Пример:

arr = [1, 2, 3, 4, 5] 

результат: [5, 4, 3, 2, 1]

### 19 (4.8). Объединение.

Формулировка: Дана сигнатура метода: public int[] concat (int[] arr1,int[] arr2); Необходимо реализовать метод таким образом, чтобы он возвращал новый массив, в котором сначала идут элементы первого массива (arr1), а затем второго (arr2).

Алгоритм: Создаётся новый массив длиной суммы длин первых двух. Через цикл элементы сначала первого массива заносятся в новый, затем второго. Новый массив возвращается.

Пример:

arr1 = [1, 2, 3] 

arr2 = [7, 8, 9] 

результат: [1, 2, 3, 7, 8, 9]

### 20 (4.10). Удалить негатив.

Формулировка: Дана сигнатура метода: public int[] deleteNegative (int[] arr); Необходимо реализовать метод таким образом, чтобы он возвращал новый массив, в котором записаны все элементы массива arr кроме отрицательных.

Алгоритм: Создаётся переменная для подсчёта неотрицательных элементов. Через цикл выполняется проход по массиву и подсчёт неотрицательных элементов. Создаётся новый массив размера заведённой переменной. Выполняется второй проход по старому массиву, вне неотрицательные элементы записываются в новый массив. Новый массив возвращается. 

Пример:

arr = [1, 2, -3, 4, -2, 2, -5] 

результат: [1, 2, 4, 2]


## Автор
Костицын Семён, ИТ-11-12-2024
